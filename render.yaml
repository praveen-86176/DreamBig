services:
  - type: web
    name: dreambig
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5001
      - key: GEMINI_API_KEY
        sync: false
      - key: MONGODB_URI
        sync: false
    healthCheckPath: /api/health
